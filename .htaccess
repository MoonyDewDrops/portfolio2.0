RewriteEngine On

# If the requimage.png
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Rewrite /about to /about.php, /projects to /projects.php, etc.
RewriteRule ^([a-zA-Z0-9_-]+)$ $1.php [L] 